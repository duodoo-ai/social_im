<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Tree View -->
        <record id="view_ai_tool_list" model="ir.ui.view">
            <field name="name">oudu.bot.tool.list</field>
            <field name="model">oudu.bot.tool</field>
            <field name="arch" type="xml">
                <list>
                    <field name="sequence" widget="handle"/>
                    <field name="name"/>
                    <field name="code"/>
                    <field name="tool_type"/>
                    <field name="function_name"/>
                    <field name="is_active"/>
                    <field name="last_demo_status" widget="badge"
                           options="{'success': 'success', 'warning': 'not_tested', 'danger': 'failed'}"/>
                </list>
            </field>
        </record>

        <!-- Form View -->
        <record id="view_ai_tool_form" model="ir.ui.view">
            <field name="name">oudu.bot.tool.form</field>
            <field name="model">oudu.bot.tool</field>
            <field name="arch" type="xml">
                <form>
                    <header>
                        <button name="execute_demo_tool"
                            type="object"
                            string="Test Demo"
                            class="btn-secondary"/>
                        <button name="clear_demo_history"
                            type="object"
                            string="Clear History"
                            class="btn-warning"
                            invisible="not demo_execution_history"/>
                        <field name="is_active" widget="boolean_toggle"/>
                    </header>
                    <sheet>
                        <group>
                            <group>
                                <field name="sequence"/>
                                <field name="name"/>
                                <field name="code"/>
                                <field name="tool_type"/>
                                <field name="function_name"/>
                            </group>
                            <group>
                                <field name="is_active"/>
                                <field name="require_authentication"/>
                                <field name="allowed_groups" widget="many2many_tags"/>
                                <field name="last_demo_status" widget="badge"
                                       options="{'success': 'success', 'warning': 'not_tested', 'danger': 'failed'}"/>
                            </group>
                        </group>

                        <group string="Description">
                            <field name="description" nolabel="1"/>
                        </group>

                        <notebook>
                            <!-- 演示执行结果页面 -->
                            <page string="Demo Results">
                                <group string="Execution History">
                                    <field name="demo_execution_history" nolabel="1">
                                        <list>
                                            <field name="execution_date"/>
                                            <field name="is_success" widget="boolean"/>
                                            <field name="arguments" widget="text"/>
                                            <field name="result" widget="text"/>
                                        </list>
                                        <form>
                                            <group>
                                                <group>
                                                    <field name="execution_date" readonly="1"/>
                                                    <field name="is_success" readonly="1"/>
                                                </group>
                                                <group>
                                                    <field name="tool_id" readonly="1"/>
                                                </group>
                                            </group>
                                            <group string="Arguments">
                                                <field name="arguments" nolabel="1" readonly="1" widget="text"/>
                                            </group>
                                            <group string="Result">
                                                <field name="result" nolabel="1" readonly="1" widget="text"/>
                                            </group>
                                        </form>
                                    </field>
                                </group>
                            </page>

                            <page string="Parameters">
                                <group>
                                    <field name="parameters" widget="json" nolabel="1"/>
                                </group>
                            </page>

                            <page string="Implementation">
                                <group>
                                    <field name="tool_type" invisible="1"/>

                                    <group string="Python Code" invisible="tool_type != 'python'">
                                        <field name="python_code" nolabel="1" options="{'mode': 'python'}"/>
                                    </group>

                                    <group string="Odoo Method" invisible="tool_type != 'odoo_method'">
                                        <field name="odoo_model"/>
                                        <field name="odoo_method"/>
                                    </group>

                                    <group string="API Call" invisible="tool_type != 'api'">
                                        <field name="api_endpoint"/>
                                        <field name="api_method" widget="radio" options="{'horizontal': true}"/>
                                    </group>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Action -->
        <record id="action_ai_tool" model="ir.actions.act_window">
            <field name="name">AI Tools</field>
            <field name="res_model">oudu.bot.tool</field>
            <field name="view_mode">list,form</field>
            <field name="view_id" ref="view_ai_tool_list"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first AI Tool!
                </p>
            </field>
        </record>
    </data>
</odoo>